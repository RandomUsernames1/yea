<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Bootable USBs - Next Gen</title>
<style>
    /* ---------------- GLOBAL STYLES ---------------- */
    * {margin:0; padding:0; box-sizing:border-box; font-family:-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;}
    body {background:#0f0f0f; color:#fff; overflow-x:hidden; scroll-behavior:smooth; cursor:none;}
    a {text-decoration:none; color:inherit;}
    header {width:100%; padding:20px 50px; display:flex; justify-content:space-between; align-items:center; position:fixed; top:0; left:0; background:rgba(15,15,15,0.85); backdrop-filter:blur(5px); z-index:100;}
    header .logo {font-size:1.5rem; font-weight:700; letter-spacing:2px; padding:6px 12px; border:1px solid #fff; border-radius:6px;}
    header nav a {margin-left:30px; font-weight:500; transition:color 0.3s;}
    header nav a:hover {color:#FFD700;}
    .hero {height:100vh; display:flex; flex-direction:column; justify-content:center; align-items:center; text-align:center; padding:0 20px; background:url('https://i.postimg.cc/43gjm9CH/pic.avif') no-repeat center center; background-size:cover; position:relative;}
    .hero::after {content:''; position:absolute; inset:0; background:rgba(0,0,0,0.6); transition:background 0.3s ease;}
    body.hovered .hero::after {background:rgba(0,0,0,0.3);}
    .hero-content {position:relative; z-index:1; max-width:900px;}
    .hero h1 {font-size:4rem; font-weight:700; margin-bottom:20px; transition:transform 0.2s;}
    .hero h1:hover {transform:scale(1.03);}
    .hero p {font-size:1.5rem; line-height:1.5; margin-bottom:40px; transition:transform 0.2s;}
    .hero p:hover {animation: tiltWiggle 0.4s infinite ease-in-out; transform:scale(1.03);}
    @keyframes tiltWiggle {0% {transform:scale(1.03) rotate(0deg);}25% {transform:scale(1.03) rotate(2deg);}50% {transform:scale(1.03) rotate(0deg);}75% {transform:scale(1.03) rotate(-2deg);}100% {transform:scale(1.03) rotate(0deg);}}
    .glass-button {backdrop-filter:blur(12px); background:rgba(255,255,255,0.1); border-radius:30px; padding:20px 60px; border:2px solid rgba(255,255,255,0.25); box-shadow:0 8px 32px rgba(0,0,0,0.3); font-size:1.5rem; font-weight:700; color:#fff; transition:all 0.3s ease; position:relative; overflow:hidden; cursor:pointer;}
    .glass-button::before {content:''; position:absolute; top:0; left:-75%; width:50%; height:100%; background:linear-gradient(120deg, rgba(255,255,255,0) 0%, rgba(255,255,255,0.4) 50%, rgba(255,255,255,0) 100%); transform:skewX(-20deg);}
    .glass-button:hover::before {animation: shimmer 1s forwards;}
    @keyframes shimmer {0% {left:-75%;}100% {left:125%;}}
    .glass-button:hover {background:rgba(255,255,255,0.25); transform:scale(1.05); box-shadow:0 12px 40px rgba(0,0,0,0.4);}
    .features {padding:100px 50px; display:flex; flex-wrap:wrap; justify-content:center; gap:50px; background:#0f0f0f;}
    .feature-card {position:relative; background:rgba(255,255,255,0.05); border-radius:20px; padding:40px; width:300px; text-align:center; backdrop-filter:blur(8px); transition:transform 0.3s, box-shadow 0.3s, background 0.3s; overflow:hidden;}
    .feature-card::before {content:''; position:absolute; top:0; left:-75%; width:50%; height:100%; background:linear-gradient(120deg, rgba(255,255,255,0) 0%, rgba(255,255,255,0.3) 50%, rgba(255,255,255,0) 100%); transform:skewX(-20deg);}
    .feature-card.animate-once::before {animation: shimmer 1s forwards;}
    .feature-card.animate-once {background:rgba(255,255,255,0.15); transform:translateY(-10px); box-shadow:0 12px 30px rgba(0,0,0,0.5);}
    .feature-card:hover::before {animation: shimmer 1s forwards;}
    .feature-card:hover {background:rgba(255,255,255,0.15); transform:translateY(-10px); box-shadow:0 12px 30px rgba(0,0,0,0.5);}
    .feature-card h3 {margin-bottom:20px; font-size:1.5rem;}
    .feature-card p {font-size:1rem; line-height:1.5; color:#ccc;}
    .contact {padding:100px 50px; text-align:center; font-size:1.5rem;}
    footer {text-align:center; padding:20px; background:#0f0f0f; font-size:0.9rem; color:#888;}

    /* ---------------- LIQUID GLASS CURSOR ---------------- */
    .cursor {position:fixed; top:0; left:0; width:60px; height:60px; border-radius:50%; pointer-events:none; z-index:9999; background:rgba(255,255,255,0.1); backdrop-filter:blur(25px) saturate(250%); border:2px solid rgba(255,255,255,0.3); box-shadow:0 0 15px rgba(255,255,255,0.2), inset 0 0 10px rgba(255,255,255,0.3); transform:translate(-50%, -50%); transition: width 0.2s, height 0.2s, background 0.2s, border 0.2s; mix-blend-mode:screen; will-change:transform; overflow:hidden;}
    .cursor::before {content:''; position:absolute; top:10%; left:10%; width:80%; height:80%; border-radius:50%; background:radial-gradient(circle at 30% 30%, rgba(255,255,255,0.4), rgba(255,255,255,0) 70%);}
    .cursor::after {content:''; position:absolute; top:20%; left:20%; width:60%; height:60%; border-radius:50%; background:radial-gradient(circle at 70% 30%, rgba(255,255,255,0.2), rgba(255,255,255,0) 70%);}
    .cursor.active {width:100px; height:100px; background:rgba(255,255,255,0.2); border-color:rgba(255,255,255,0.5); box-shadow:0 0 25px rgba(255,255,255,0.3), inset 0 0 15px rgba(255,255,255,0.4);}

    /* ---------------- PAGE LOADER ---------------- */
    .page-loader {position:fixed; bottom:0; left:0; width:100%; height:100%; background:#000; z-index:99999; clip-path:inset(0 0 100% 0); animation:revealUp 1.5s ease forwards;}
    @keyframes revealUp {0% {clip-path:inset(0 0 100% 0);}100% {clip-path:inset(100% 0 0 0);}}

    /* ---------------- SHOP MODAL ---------------- */
    .shop-modal {
        position:fixed;
        bottom:20px;
        right:20px;
        background:rgba(30,30,30,0.95);
        padding:30px 40px;
        border-radius:15px;
        text-align:center;
        box-shadow:0 10px 40px rgba(0,0,0,0.6);
        z-index:999;
        width:300px;
        transition: all 0.5s ease;
        overflow:hidden;
        border: 2px solid rgba(0,255,0,0.5);
    }
    .shop-modal h2 {margin-bottom:20px; font-size:1.8rem; color:#FFD700; text-decoration:underline;}
    .shop-modal a {display:block; margin:12px 0; font-size:1.4rem; color:#fff; transition:color 0.2s; position:relative;}
    .shop-modal:hover {box-shadow: 0 0 30px rgba(0,255,0,0.8), 0 0 50px rgba(0,255,0,0.4) inset; border-color: rgba(255,0,255,0.8);}
    .shop-modal::before {content:''; position:absolute; top:-2px; left:-2px; width:calc(100% + 4px); height:calc(100% + 4px); border:2px solid limegreen; border-radius:15px; pointer-events:none; background: linear-gradient(90deg, transparent, limegreen, transparent); background-size:200% 100%; animation: borderMove 2s linear infinite; opacity:0; transition: opacity 0.3s;}
    .shop-modal:hover::before {opacity:1;}
    @keyframes borderMove {0% {background-position:200% 0;}100% {background-position:-200% 0;}}

    /* ---------------- MARKETING BANNER ---------------- */
    .marketing-banner {
        position:fixed;
        top:0;
        left:50%;
        transform:translateX(-50%);
        background: rgba(255,255,255,0.9);
        padding:5px 15px;
        border-radius:20px;
        font-size:1rem;
        font-weight:600;
        color:#000;
        display:flex;
        gap:20px;
        z-index:200;
        box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        overflow:hidden;
        pointer-events:none;
        transition: all 0.5s ease;
    }
    .marketing-message {white-space: nowrap;}
</style>
</head>
<body>

<!-- PAGE LOADER -->
<div class="page-loader"></div>

<header>
    <div class="logo">Bootable USBs</div>
    <nav>
        <a id="featuresBtn">Features</a>
        <a id="contactBtn">Contact</a>
    </nav>
</header>

<!-- MARKETING BANNER -->
<div class="marketing-banner" id="marketingBanner"></div>

<section class="hero">
    <div class="hero-content">
        <h1>Next Generation Bootable USBs.</h1>
        <p>No tech skills? No problem. Boot up easily.</p>
        <div class="glass-button" id="shopBtn">Shop Now</div>
    </div>
</section>

<section class="features" id="features">
    <div class="feature-card">
        <h3>Ultra Fast</h3>
        <p>Boot your OS in seconds with our high-speed USB drives.</p>
    </div>
    <div class="feature-card">
        <h3>Durable Design</h3>
        <p>Premium materials built to last, with stylish aesthetics.</p>
    </div>
    <div class="feature-card">
        <h3>Cross-Platform</h3>
        <p>Compatible with Windows, Linux, and macOS boot environments.</p>
    </div>
</section>

<section class="contact" id="contact">
    <p id="contactText">Contact us at: <strong>example@email.com</strong></p>
</section>

<footer>
    &copy; 2025 Bootable USBs. All rights reserved.
</footer>

<!-- Bottom-Right Shop Modal -->
<div class="shop-modal" id="shopModal">
    <h2>Shop Options</h2>
    <a href="https://www.etsy.com" target="_blank">Etsy</a>
    <a href="https://www.ebay.com" target="_blank">eBay</a>
    <a href="https://www.facebook.com" target="_blank">Facebook</a>
</div>

<!-- Liquid glass cursor -->
<div class="cursor"></div>

<script>
    /* ---------------- PAGE LOADER ---------------- */
    const loader = document.querySelector('.page-loader');
    loader.addEventListener('animationend', () => loader.style.display='none');

    /* ---------------- BUTTON & NAV INTERACTIONS ---------------- */
    const button = document.querySelector('.glass-button');
    button.addEventListener('mouseenter',()=>document.body.classList.add('hovered'));
    button.addEventListener('mouseleave',()=>document.body.classList.remove('hovered'));

    const featuresBtn = document.getElementById('featuresBtn');
    const cards = document.querySelectorAll('.feature-card');
    featuresBtn.addEventListener('click', ()=>{
        document.getElementById('features').scrollIntoView({behavior:'smooth'});
        cards.forEach(card=>{card.classList.add('animate-once'); setTimeout(()=>card.classList.remove('animate-once'),1200);});
    });

    document.getElementById('contactBtn').addEventListener('mouseenter',()=>{
        const text=document.getElementById('contactText');
        document.getElementById('contact').scrollIntoView({behavior:'smooth'});
        text.classList.add('bounce');
        setTimeout(()=>text.classList.remove('bounce'),600);
    });

    /* ---------------- LIQUID GLASS CURSOR ---------------- */
    const cursor=document.querySelector('.cursor');
    let mouseX=0, mouseY=0, posX=0, posY=0;
    document.addEventListener('mousemove',e=>{mouseX=e.clientX; mouseY=e.clientY;});
    function animate(){posX+=(mouseX-posX)*0.15; posY+=(mouseY-posY)*0.15; cursor.style.transform=`translate(${posX}px,${posY}px) translate(-50%,-50%)`; requestAnimationFrame(animate);}
    animate();
    const interactive=document.querySelectorAll('a, button, .glass-button');
    interactive.forEach(el=>{el.addEventListener('mouseenter',()=>cursor.classList.add('active')); el.addEventListener('mouseleave',()=>cursor.classList.remove('active'));});

    /* ---------------- SHOP MODAL ---------------- */
    const shopModal = document.getElementById('shopModal');
    const shopBtn = document.getElementById('shopBtn');
    const banner = document.getElementById('marketingBanner');
    const bannerMessages = ["Almost Sold Out!", "Buy it Quickly!", "Best Rated!", "Top of the Best!"];

    function showBannerMessages() {
        banner.innerHTML = '';
        bannerMessages.forEach(msg => {
            const span = document.createElement('div');
            span.className = 'marketing-message';
            span.textContent = msg;
            banner.appendChild(span);
        });
    }
    showBannerMessages();

    shopBtn.addEventListener('click', () => {
        const btnRect = shopBtn.getBoundingClientRect();
        const modalRect = shopModal.getBoundingClientRect();
        const centerX = btnRect.left + btnRect.width/2 - modalRect.width/2;
        const centerY = btnRect.top + btnRect.height + 10; // just below button
        shopModal.style.bottom = 'auto';
        shopModal.style.right = 'auto';
        shopModal.style.top = `${centerY}px`;
        shopModal.style.left = `${centerX}px`;

        // Move banner above modal
        banner.style.top = `${centerY - 40}px`;
    });

</script>

</body>
</html>
